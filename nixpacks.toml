# https://nixpacks.com/docs/configuration

name = "happysmile"

[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "node prod-server.mjs"
